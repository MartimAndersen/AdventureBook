spring:
  application:
    name: adventure-book

  datasource:
    url: jdbc:postgresql://127.0.0.1:5433/adventure_book
    username: postgres
    password: postgres

  jpa:
    hibernate:
      ddl-auto: validate

  flyway:
    enabled: true